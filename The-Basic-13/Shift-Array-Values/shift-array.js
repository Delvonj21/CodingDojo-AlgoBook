// Given an array, move all values forward by one index, dropping the first and leaving a ​'0'​ value at the end.

const myArray = [1, 2, 3, 4, 5]; 
myArray.shift(); //Remove the first element 
myArray.push(0); //adds '0' at the end 
